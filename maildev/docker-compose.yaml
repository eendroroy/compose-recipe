services:
  maildev:
    image: maildev/maildev
    container_name: maildev
    restart: unless-stopped
    ports:
      - "1025:1025"   # SMTP port — your app sends mail here
      - "1080:1080"   # Web UI — view caught emails here
